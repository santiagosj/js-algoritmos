//ORDENAMIENTO POR INSERCION DIRECTA

/*
public static void insercionDirecta(int[]a){
   for(int i = 1; i < a.lenght; i++){
     int aux = a[i];
     int j = i - 1;
     while(j >= 0 && aux < a[j] ){
       a[j + 1] = a[j];
       j--;
    }
    a[j + 1] = aux;
  } //for
}
*/
